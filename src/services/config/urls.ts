
export const docSearchServerPath = (url: string): string => {
    return "/doc-search-server/" + url;
};

export const migrationServerPath = (url: string): string => {
    return "/doc-search-migration-server/" + url;
};

export const securityServerPath = (url: string): string => {
    return "/security-server/" + url;
};

